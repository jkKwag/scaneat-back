package com.scaneat.back.repository;

import com.scaneat.back.entity.BizSubspt;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BizSubsptRepository extends JpaRepository<BizSubspt, String> {
}
